import {useState} from 'react';import {useSuspenseQuery} from '@tanstack/react-query';import {storefrontQuery,type StoreProduct} from '@/lib/storefront';import {usePersistentCart} from '@/lib/usePersistentCart';import {createOrder,type OrderSummary} from '@/lib/orders';import {initializeOrderPayment} from '@/lib/payments';import {Button,Badge,Card} from '@/components/ui/primitives';import {ChevronLeft,ChevronRight,Menu,MessageCircle,Minus,Plus,Search,ShoppingBag,X} from 'lucide-react';import {MetaPixel,trackBrowserCommerce} from './MetaPixel';
export function DynamicStorefront({vendorSlug,initialProductSlug}:{vendorSlug:string;initialProductSlug?:string}){const {data}=useSuspenseQuery(storefrontQuery(vendorSlug));const vendor=data.vendor;const products=data.products;const initialProduct=products.find(product=>product.slug===initialProductSlug);const {lines:cart,add:addToCart,updateQuantity,clear:clearCart}=usePersistentCart(vendorSlug,products);const [drawer,setDrawer]=useState(false),[selected,setSelected]=useState<StoreProduct|undefined>(initialProduct),[variant,setVariant]=useState(0),[country,setCountry]=useState('Nigeria'),[deliveryState,setDeliveryState]=useState(data.shippingZones[0]?.state??''),[zone,setZone]=useState(data.shippingZones[0]?.zone_name??''),[sidebar,setSidebar]=useState(false),[customer,setCustomer]=useState({name:'',email:'',phone:'',address:'',note:''}),[checkoutError,setCheckoutError]=useState(''),[submitting,setSubmitting]=useState(false),[order,setOrder]=useState<OrderSummary|null>(null),[checkoutToken,setCheckoutToken]=useState(()=>crypto.randomUUID());if(data.notFound||!vendor)return <main className="grid min-h-screen place-items-center bg-background p-5"><Card className="max-w-lg p-9 text-center"><h1 className="text-3xl font-bold">Store not found</h1><p className="mt-3 text-muted-foreground">This address may have changed or the store is not currently published.</p></Card></main>;if(data.locked)return <main className="grid min-h-screen place-items-center bg-background p-5"><Card className="max-w-xl p-9 text-center"><div className="mx-auto grid size-16 place-items-center rounded-full bg-secondary text-2xl">{vendor.design_settings.logo_text}</div><h1 className="mt-6 text-3xl font-bold">{vendor.business_name} is refreshing its store</h1><p className="mt-3 leading-7 text-muted-foreground">Online ordering is temporarily unavailable. You can still contact the business directly for assistance.</p><a href={`https://wa.me/${vendor.whatsapp_number}?text=${encodeURIComponent(`Hello ${vendor.business_name}, I would like to make an enquiry.`)}`} className="mt-7 inline-flex rounded-full bg-primary px-6 py-3 font-bold text-primary-foreground">Chat on WhatsApp</a></Card></main>;const selectedVariants=selected?.variants??[];const intl=country!=='Nigeria',countryCode=country==='Nigeria'?'NG':country==='United Kingdom'?'GB':country==='United States'?'US':'CA',rate=1600,allDigital=cart.length>0&&cart.every(item=>item.product.product_type==='digital'||item.product.product_type==='booking');const availableStates=[...new Set(data.shippingZones.filter(item=>item.country_code==='NG').map(item=>item.state))];const chosenZone=data.shippingZones.find(item=>item.state===deliveryState&&item.zone_name===zone);const standardShipping=intl?35*rate:chosenZone?.fee??0;const cargoProfile=data.cargoProfiles.find(profile=>profile.destination_country===countryCode);const cargoShipping=vendor.business_category==='cargo_logistics'&&cargoProfile?Math.max(cargoProfile.minimum_charge,cart.reduce((sum,item)=>sum+item.product.weight_kg*item.quantity,0)*cargoProfile.rate):standardShipping;const shipping=allDigital?0:cargoShipping;const subtotal=cart.reduce((sum,item)=>sum+(item.product.base_price+item.priceModifier)*item.quantity,0);const total=subtotal+shipping;const money=(amount:number)=>intl?`$${(amount/rate).toFixed(2)}`:`₦${amount.toLocaleString()}`;const add=(product:StoreProduct,variantId:string|null)=>{addToCart(product,variantId);trackBrowserCommerce('AddToCart',vendorSlug,vendor.default_currency,product.base_price,[product.id]);setDrawer(true)};const placeOrder=async(provider:'paystack'|'flutterwave'|'stripe'|'bank_transfer'|'whatsapp')=>{setCheckoutError('');if(customer.name.trim().length<2||!/^\S+@\S+\.\S+$/.test(customer.email)){setCheckoutError('Enter your full name and a valid email address.');return}if(!allDigital&&customer.address.trim().length<8){setCheckoutError('Enter a complete delivery address.');return}if(!allDigital&&countryCode==='NG'&&!chosenZone){setCheckoutError('Choose an available delivery zone.');return}trackBrowserCommerce('InitiateCheckout',vendorSlug,vendor.default_currency,total,cart.map(item=>item.product.id));setSubmitting(true);const result=await createOrder({data:{vendorSlug,checkoutToken,customer:{name:customer.name,email:customer.email,phone:customer.phone,note:customer.note},destination:{country_code:countryCode,state:chosenZone?.state,zone:chosenZone?.zone_name,address:customer.address},zoneId:countryCode==='NG'?chosenZone?.id??null:null,provider,items:cart.map(item=>({product_id:item.product.id,variant_id:item.variantId,quantity:item.quantity}))}});if(!result.ok||!result.order){setSubmitting(false);setCheckoutError(result.error??'Unable to create order.');return}if(provider==='paystack'||provider==='flutterwave'){const payment=await initializeOrderPayment({data:{orderId:result.order.order_id,checkoutToken,provider}});if(!payment.ok||!payment.authorizationUrl){setSubmitting(false);setCheckoutError(payment.error??'Unable to open secure payment.');return}clearCart();setCheckoutToken(crypto.randomUUID());window.location.assign(payment.authorizationUrl);return}setSubmitting(false);setOrder(result.order);if(provider==='whatsapp'){const text=`Hello ${vendor.business_name}, my order NDH-${result.order.order_number} contains: ${cart.map(item=>`${item.quantity}× ${item.product.name}${item.variantLabel?` (${item.variantLabel})`:''}`).join(', ')}. Delivery: ${zone||'Not required'}. Total: ${money(total)}.`;window.open(`https://wa.me/${vendor.whatsapp_number}?text=${encodeURIComponent(text)}`)}clearCart();setCheckoutToken(crypto.randomUUID())};const whatsapp=()=>void placeOrder('whatsapp');const bookingBusiness=['travel_tours','freelance_services','event_ticketing'].includes(vendor.business_category);const categories=[...new Set(products.map(product=>product.category))];const navLinks=vendor.design_settings.enabled_pages.length?vendor.design_settings.enabled_pages:['Shop','About','Contact'];const heroImage=products.find(product=>product.is_featured)?.image_url||products[0]?.image_url||'/hero-fashion-real.jpg';const heroCopy=vendor.business_category==='cargo_logistics'?['GLOBAL LOGISTICS','Move anything. Anywhere.','Reliable cargo routes with transparent weight and volume pricing.']:bookingBusiness?['BOOK YOUR EXPERIENCE','Time well spent, beautifully planned.','Explore available services, dates and experiences created for you.']:vendor.business_category==='grocery_food'?['FRESH THIS WEEK','Better food, closer to home.','Shop fresh essentials from a business you know and trust.']:['THE SIGNATURE EDIT','Designed for the way you live.','Discover considered products selected with quality and confidence.'];const logo=<>{vendor.design_settings.logo_url?<img src={vendor.design_settings.logo_url} alt={vendor.business_name} className="size-full object-cover"/>:vendor.design_settings.logo_text}</>;const headerClass=vendor.design_settings.nav_style==='floating_island'?'sticky top-3 mx-auto mt-3 w-[calc(100%-24px)] max-w-6xl rounded-full border border-white/60 bg-card/85 shadow-lg backdrop-blur-xl':vendor.design_settings.nav_style==='centered'?'relative mx-auto mt-2 max-w-7xl border-b border-border bg-card':'relative w-full border-b border-border bg-card';return <main className="min-h-screen bg-background text-foreground" style={{'--store-accent':vendor.design_settings.primary_accent,'--store-background':vendor.design_settings.background_accent,backgroundColor:'var(--store-background)'} as React.CSSProperties}><MetaPixel pixelId={vendor.meta_pixel_id} vendorSlug={vendorSlug} productId={selected?.id} currency={vendor.default_currency} value={selected?.base_price}/><div className="overflow-hidden whitespace-nowrap bg-[var(--store-accent)] py-2 text-center text-xs font-bold text-white"><span className="inline-block animate-pulse">{vendor.design_settings.announcement_text} &nbsp; ✦ &nbsp; {vendor.design_settings.announcement_text}</span></div><header className={`z-30 flex items-center justify-between px-5 py-3 ${headerClass}`}><button onClick={()=>setSidebar(true)} className={vendor.design_settings.nav_style==='sidebar'?'block':'md:hidden'}><Menu/></button><button onClick={()=>setSelected(undefined)} className="flex items-center gap-3"><span className={`grid size-10 place-items-center overflow-hidden bg-[var(--store-accent)] font-display text-white ${vendor.design_settings.logo_shape==='circle'?'rounded-full':'rounded-xl'}`}>{logo}</span><b className="hidden sm:block">{vendor.business_name.toUpperCase()}</b></button><nav className={`${vendor.design_settings.nav_style==='sidebar'?'hidden':'hidden gap-6 text-sm md:flex'}`}>{navLinks.map(link=><button key={link}>{link}</button>)}</nav><div className="flex gap-4"><Search className="size-5"/><button onClick={()=>setDrawer(true)} className="relative"><ShoppingBag className="size-5"/>{cart.length>0&&<span className="absolute -right-2 -top-2 grid size-4 place-items-center rounded-full bg-accent text-[10px]">{cart.length}</span>}</button></div></header>{sidebar&&<div className="fixed inset-0 z-50 bg-white p-6"><button onClick={()=>setSidebar(false)} className="float-right"><X/></button><b className="text-xl">{vendor.business_name.toUpperCase()}</b><nav className="mt-16 space-y-7 text-3xl">{navLinks.map(link=><button key={link} className="block">{link}</button>)}</nav></div>}
{selected?<section className="mx-auto max-w-7xl px-5 py-12"><button onClick={()=>setSelected(undefined)} className="flex items-center gap-2 text-sm"><ChevronLeft className="size-4"/>Back to collection</button><div className="mt-7 grid gap-10 lg:grid-cols-2"><div className="grid gap-3 sm:grid-cols-2">{(selected.image_urls.length?selected.image_urls:[{url:selected.image_url,path:''}]).map((image,index)=><img key={`${image.url}-${index}`} src={image.url} alt={`${selected.name} view ${index+1}`} className={`w-full object-cover ${index===0?'h-[480px] sm:col-span-2 sm:h-[580px]':'h-64'}`}/>)}</div><div className="lg:sticky lg:top-28 lg:self-start"><Badge>{selected.category}</Badge><h1 className="mt-4 font-display text-5xl">{selected.name}</h1><p className="mt-4 text-2xl">{money(selected.base_price+(selectedVariants[variant]?.price_modifier||0))}</p><p className="mt-6 max-w-lg leading-7 text-muted-foreground">{selected.description} Each piece is selected for natural movement, effortless styling and enduring quality.</p>{selectedVariants.length>0&&<div className="mt-7"><b className="text-sm">Choose length</b><div className="mt-3 flex gap-2">{selectedVariants.map((v,i)=><button onClick={()=>setVariant(i)} className={`rounded-full border px-4 py-2 text-sm ${variant===i?'bg-[var(--store-accent)] text-white':''}`}>{v.variant_value}</button>)}</div></div>}<Button onClick={()=>add(selected,selectedVariants[variant]?.id??null)} className="mt-8 w-full rounded-none bg-[var(--store-accent)] py-4">{selected.product_type==='booking'||selected.product_type==='service'?'Reserve now':selected.product_type==='digital'?'Buy digital product':'Add to bag'}</Button><div className="mt-7 divide-y border-y text-sm">{['Complimentary styling consultation','Delivery across Nigeria','Authenticity guaranteed'].map(x=><div className="flex items-center gap-2 py-4">✦ {x}</div>)}</div></div></div></section>:<><section className="mx-auto max-w-7xl px-5 pb-14 pt-10"><div className="relative overflow-hidden bg-[var(--store-accent)] text-white"><img className="absolute inset-0 h-full w-full object-cover opacity-45" src={heroImage}/><div className="relative max-w-2xl px-7 py-24 sm:px-14 sm:py-36"><p className="text-xs font-bold tracking-[.25em]">{heroCopy[0]}</p><h1 className="mt-4 font-display text-5xl leading-tight sm:text-7xl">{heroCopy[1]}</h1><p className="mt-5 max-w-md leading-7 text-white/80">{heroCopy[2]}</p><button className="mt-8 bg-white px-6 py-3 font-bold text-foreground">Explore collection</button></div></div></section>{bookingBusiness&&<section className="mx-auto max-w-7xl px-5 pb-10"><div className="grid gap-5 rounded-3xl border border-border bg-card p-6 lg:grid-cols-[.7fr_1.3fr]"><div><Badge>LIVE AVAILABILITY</Badge><h2 className="mt-4 font-display text-3xl">Choose your date</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">Select an experience below to see its published schedule and remaining capacity.</p></div><div className="grid gap-3 sm:grid-cols-2">{products.flatMap(product=>product.booking_slots.map(slot=>({product,slot}))).slice(0,6).map(({product,slot})=><button key={slot.id} onClick={()=>setSelected(product)} className="rounded-2xl bg-muted p-4 text-left hover:bg-secondary"><b className="block text-sm">{product.name}</b><span className="mt-2 block text-xs text-muted-foreground">{new Intl.DateTimeFormat('en-NG',{dateStyle:'medium',timeStyle:'short',timeZone:vendor.timezone}).format(new Date(slot.starts_at))}</span><span className="mt-2 block text-xs font-bold">{slot.capacity-slot.reserved_count} places available</span></button>)}{!products.some(product=>product.booking_slots.length)&&<div className="rounded-2xl bg-muted p-5 text-sm text-muted-foreground sm:col-span-2">New availability is being prepared. Contact the business through WhatsApp for the next open date.</div>}</div></div></section>}{vendor.business_category==='cargo_logistics'&&data.cargoProfiles.length>0&&<section className="mx-auto max-w-7xl px-5 pb-10"><div className="rounded-3xl bg-primary p-6 text-primary-foreground sm:p-8"><p className="text-xs font-bold tracking-wider text-accent">FREIGHT RATE PROFILES</p><div className="mt-5 grid gap-3 md:grid-cols-3">{data.cargoProfiles.map(profile=><div key={profile.id} className="rounded-2xl border border-white/15 p-4"><b>{profile.name}</b><p className="mt-2 text-2xl font-bold">{profile.currency} {profile.rate.toLocaleString()} <small className="text-xs font-normal opacity-60">/ {profile.metric.toUpperCase()}</small></p><p className="mt-2 text-xs opacity-60">{profile.origin_country} → {profile.destination_country} · {profile.transport_mode}</p></div>)}</div></div></section>}<section className="mx-auto max-w-7xl px-5 py-8"><div className="flex gap-3 overflow-x-auto pb-3">{['All pieces',...categories].map((category,index)=><button key={category} className={`shrink-0 rounded-full px-5 py-2 text-sm ${index===0?'bg-[var(--store-accent)] text-white':'border border-foreground/20'}`}>{category}</button>)}</div><div className="mt-10 flex items-end justify-between"><div><p className="text-xs font-bold tracking-[.2em]">CURATED FOR YOU</p><h2 className="mt-2 font-display text-4xl">The collection</h2></div><span className="text-sm text-muted-foreground">{products.length} pieces</span></div><div className="mt-7 grid gap-x-4 gap-y-10 sm:grid-cols-2 lg:grid-cols-3">{products.map(p=><article key={p.id}><button onClick={()=>setSelected(p)} className="group block w-full overflow-hidden bg-white"><img src={p.image_url||'/hero-fashion-real.jpg'} alt={p.name} loading="lazy" className="h-96 w-full object-cover transition duration-500 group-hover:scale-105"/></button><div className="mt-4 flex justify-between"><div><h3 className="font-bold">{p.name}</h3><p className="text-sm text-muted-foreground">{p.category}</p></div><b>{money(p.base_price)}</b></div></article>)}</div></section></>}
<footer className="mt-20 bg-[var(--store-accent)] px-5 py-16 text-white"><div className="mx-auto grid max-w-7xl gap-10 md:grid-cols-3"><div><p className="font-display text-3xl">AMARI</p><p className="mt-4 max-w-xs text-sm text-white/60">{vendor.business_description}</p></div><div><b>Visit</b><p className="mt-4 text-sm text-white/60">Shop · Our story · Delivery · Care guide</p></div><div><b>Private list</b><p className="mt-4 text-sm text-white/60">Early access and considered notes.</p><div className="mt-3 flex border-b"><input className="w-full bg-transparent py-2 outline-none" placeholder="Your email"/><ChevronRight/></div></div></div></footer><button onClick={()=>window.open(`https://wa.me/${vendor.whatsapp_number}?text=${encodeURIComponent('Hello! I have a question about an item on your store.')}`)} className="fixed bottom-5 right-5 z-30 grid size-14 place-items-center rounded-full bg-green-600 text-white shadow-xl"><MessageCircle/></button>
{drawer&&<><button className="fixed inset-0 z-40 bg-foreground/40" onClick={()=>setDrawer(false)}/><aside className="fixed inset-y-0 right-0 z-50 flex w-full max-w-md flex-col bg-card p-6 shadow-2xl"><div className="flex justify-between"><h2 className="font-display text-3xl">Your bag</h2><button onClick={()=>setDrawer(false)}><X/></button></div><div className="mt-7 flex-1 overflow-auto">{cart.length===0?order?<div className="py-16 text-center"><div className="mx-auto grid size-14 place-items-center rounded-full bg-accent text-2xl">✓</div><h3 className="mt-5 text-xl font-bold">Order NDH-{order.order_number} received</h3><p className="mt-2 text-sm leading-6 text-muted-foreground">We saved your order securely. Keep this number for your records.</p><button onClick={()=>setOrder(null)} className="mt-5 rounded-full bg-secondary px-5 py-2.5 text-sm font-bold">Continue shopping</button></div>:<div className="py-20 text-center text-muted-foreground"><ShoppingBag className="mx-auto mb-3"/>Your bag is empty.</div>:cart.map((x,i)=><div key={`${x.product.id}-${x.variantId??'base'}`} className="flex gap-4 border-b py-4"><img className="size-20 object-cover" src={x.product.image_url}/><div className="flex-1"><b>{x.product.name}</b>{x.variantLabel&&<p className="text-xs text-muted-foreground">{x.variantLabel}</p>}<p className="text-sm">{money(x.product.base_price+x.priceModifier)}</p><div className="mt-2 flex w-fit items-center gap-3 border px-2 py-1"><button onClick={()=>updateQuantity(i,x.quantity-1)} aria-label="Decrease quantity"><Minus className="size-3"/></button>{x.quantity}<button onClick={()=>updateQuantity(i,x.quantity+1)} aria-label="Increase quantity"><Plus className="size-3"/></button></div></div></div>)}</div>{cart.length>0&&<div className="border-t pt-5"><div className="grid gap-3 sm:grid-cols-2"><input value={customer.name} onChange={event=>setCustomer(current=>({...current,name:event.target.value}))} className="rounded-xl border border-border p-3 text-sm" placeholder="Full name"/><input value={customer.email} onChange={event=>setCustomer(current=>({...current,email:event.target.value}))} type="email" className="rounded-xl border border-border p-3 text-sm" placeholder="Email address"/><input value={customer.phone} onChange={event=>setCustomer(current=>({...current,phone:event.target.value}))} className="rounded-xl border border-border p-3 text-sm sm:col-span-2" placeholder="Phone or WhatsApp number"/></div>{!allDigital&&<textarea value={customer.address} onChange={event=>setCustomer(current=>({...current,address:event.target.value}))} className="mt-3 h-20 w-full rounded-xl border border-border p-3 text-sm" placeholder="Delivery address"/>}<label className="mt-4 block text-xs font-bold">DELIVERY COUNTRY<select value={country} onChange={e=>setCountry(e.target.value)} className="mt-2 w-full border p-3"><option>Nigeria</option><option>United Kingdom</option><option>United States</option><option>Canada</option></select></label>{!allDigital&&!intl&&<div className="mt-4 grid grid-cols-2 gap-2"><label className="block text-xs font-bold">STATE<select value={deliveryState} onChange={event=>{const nextState=event.target.value;setDeliveryState(nextState);setZone(data.shippingZones.find(item=>item.state===nextState)?.zone_name??'')}} className="mt-2 w-full rounded-xl border border-border bg-card p-3">{availableStates.map(state=><option key={state}>{state}</option>)}</select></label><label className="block text-xs font-bold">REGIONAL ZONE<select value={zone} onChange={event=>setZone(event.target.value)} className="mt-2 w-full rounded-xl border border-border bg-card p-3">{data.shippingZones.filter(item=>item.state===deliveryState).map(item=><option key={item.id} value={item.zone_name}>{item.zone_name} — ₦{item.fee.toLocaleString()}</option>)}</select></label></div>}<div className="mt-5 space-y-2 text-sm"><div className="flex justify-between"><span>Subtotal</span><span>{money(subtotal)}</span></div><div className="flex justify-between"><span>Shipping</span><span>{money(shipping)}</span></div><div className="flex justify-between border-t pt-3 text-lg font-bold"><span>Total</span><span>{money(total)}</span></div></div>{checkoutError&&<p role="alert" className="mt-4 rounded-xl bg-destructive/10 p-3 text-xs text-destructive">{checkoutError}</p>}<Button disabled={submitting} onClick={()=>void placeOrder(intl?'flutterwave':'paystack')} className="mt-5 w-full rounded-none bg-[var(--store-accent)]">{submitting?'Creating secure order…':intl?'Continue to Flutterwave card':'Continue to Paystack'}</Button>{!intl&&<button disabled={submitting} onClick={whatsapp} className="mt-3 w-full border border-green-700 py-3 font-bold text-green-700">Order via WhatsApp</button>}<p className="mt-3 text-center text-xs text-muted-foreground">Secure checkout · Your information stays private</p></div>}</aside></>}</main>}
